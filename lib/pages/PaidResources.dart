import 'package:flutter/material.dart';

class PaidResources extends StatelessWidget {
  static const routeName = 'paid';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Paid Resources'),
      ),
      body: Center(
        child: Text('Coming Soon ...'),
      ),
    );
  }
}
